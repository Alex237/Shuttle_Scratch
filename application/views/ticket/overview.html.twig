{% extends "layout.html.twig" %}

{% set navtab = 'tickets' %}

{% block content %}
<div class="container">
    <div class="row">
        <div class="col-md-8">
            <section class="well">
                <h3>Mes tickets</h3>
                {% for ticket in ticketAssignedToMe %}
                <p><a href="ticket/{{ ticket.idTicket }}/show">#{{ ticket.idTicket }}&nbsp;{{ ticket.title }}</a></p>
                {% else %}
                <div class="alert alert-warning">Aucun ticket ne vous est assigné</div>
                {% endfor %}

                {% for ticket in ticketOpenByMe %}
                <p>#{{ ticket.idTicket }}&nbsp;{{ ticket.title }}</p>
                {% else %}
                <div class="alert alert-warning">Vous n'avez ouvert aucun ticket</div>
                {% endfor %}

            </section>
        </div>
        <div class="col-md-4">
            <section class="well">
                <h3>Mes projets</h3>
            </section>
        </div>
    </div>
</div>
{% endblock %}